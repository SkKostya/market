import { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'register in Market',
}

export default function RegistrationLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
